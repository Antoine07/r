{
  "name": "r-programme",
  "version": "1.0.0",
  "description": "## Programme R",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "slides:build": "mkdir -p docs && for file in Slides/*.md; do npx marp \"$file\" --html --allow-local-files -o \"docs/$(basename \"${file%.md}.html\")\"; done",
    "slides:watch": "npx marp Slides --html --allow-local-files --server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@marp-team/marp-cli": "^4.2.3"
  }
}
